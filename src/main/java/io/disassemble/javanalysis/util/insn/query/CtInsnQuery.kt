package io.disassemble.javanalysis.util.insn.query

import io.disassemble.javanalysis.insn.CtInsn

/**
 * A class representing a query for an [CtInsn].
 * Used for filtering in lists.
 */
class CtInsnQuery : InsnQuery<CtInsn>()